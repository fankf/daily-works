server:
  port: 9006
spring:
  application:
    name: config-server
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fankf/gittest
#          search-paths: bean
          username:
          password:
          default-label: master
